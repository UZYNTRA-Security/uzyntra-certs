import {selectOrganizationAction} from "@/lib/issuer/organization-actions";
export function OrganizationSwitcher({current,organizations}:{current:string;organizations:Array<{id:string;name:string;slug:string}>}){return <form action={selectOrganizationAction} className="mt-3 flex flex-wrap items-center gap-2"><label htmlFor="organization" className="text-xs text-muted-foreground">Current organization</label><select id="organization" name="organization" defaultValue={current} className="rounded-md border bg-background px-2 py-1 text-sm text-foreground">{organizations.map(o=><option key={o.id} value={o.slug}>{o.name}</option>)}</select><button className="rounded-md border px-2 py-1 text-xs text-primary">Switch</button></form>}
