import {requireIssuerPage} from "@/lib/issuer/guards";
import {IssuerNav} from "@/components/issuer/issuer-nav";
import {OrganizationSwitcher} from "@/components/issuer/organization-switcher";
export default async function IssuerLayout({children}:{children:React.ReactNode}){const{organization,member,memberships}=await requireIssuerPage();return <div className="mx-auto max-w-7xl space-y-8 px-6 py-12"><header><p className="text-xs uppercase tracking-[.2em] text-primary">Organization issuer console</p><div className="mt-3 flex flex-wrap items-end justify-between gap-4"><div><h1 className="text-3xl font-semibold">Credential operations</h1><p className="mt-2 text-sm text-muted-foreground">{organization.name} · {member.role.toLowerCase()} · {organization.verified_status.toLowerCase()}</p><OrganizationSwitcher current={organization.slug} organizations={memberships.map(v=>v.organization)}/></div><IssuerNav/></div></header>{children}</div>}
